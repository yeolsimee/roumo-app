import 'package:flutter/material.dart';

class RoutineUpdateScreen extends StatelessWidget {
  const RoutineUpdateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
